<%- include('../partials/header', { title: title }) %>
<section class="auth-section">
  <div class="auth-card">
    <a href="/" class="nav-logo">المُميز</a>
    <h2>إنشاء حساب جديد</h2>
    <p>انضم إلى منصة المُميز التعليمية</p>

    <% if (typeof error !== 'undefined' && error) { %>
    <div class="auth-error">
      <i class="fas fa-exclamation-circle"></i> <%= error %>
    </div>
    <% } %>

    <form id="registerForm" onsubmit="return handleRegister(event)">
      <div class="form-group">
        <label>اسم الطالب <span style="color:var(--danger);">*</span></label>
        <input type="text" name="name" id="regName" placeholder="الاسم كاملاً" required>
      </div>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;">
        <div class="form-group">
          <label>رقم هاتف الطالب <span style="color:var(--text-muted);">(اختياري)</span></label>
          <input type="tel" name="phone" id="regPhone" placeholder="مثال: 01234567890">
        </div>
        <div class="form-group">
          <label>رقم ولي الأمر <span style="color:var(--danger);">*</span></label>
          <input type="tel" name="parentPhone" id="regParentPhone" placeholder="مثال: 01234567890" required>
        </div>
      </div>

      <div class="form-group">
        <label>البريد الإلكتروني <span style="color:var(--danger);">*</span></label>
        <input type="email" name="email" id="regEmail" placeholder="example@email.com" dir="ltr" required>
      </div>

      <div class="form-group">
        <label>كلمة المرور <span style="color:var(--danger);">*</span></label>
        <input type="password" name="password" id="regPassword" placeholder="كلمة المرور" required minlength="6">
      </div>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;">
        <div class="form-group">
          <label>المرحلة الدراسية <span style="color:var(--danger);">*</span></label>
          <select name="stage" id="regStage" required onchange="updateGrades()">
            <option value="">اختر المرحلة</option>
            <option value="إعدادية">إعدادية</option>
            <option value="ثانوية">ثانوية</option>
          </select>
        </div>
        <div class="form-group">
          <label>الصف الدراسي <span style="color:var(--danger);">*</span></label>
          <select name="grade" id="regGrade" required>
            <option value="">اختر الصف</option>
          </select>
        </div>
      </div>

      <div class="form-group">
        <label>المحافظة <span style="color:var(--danger);">*</span></label>
        <select name="governorate" id="regGovernorate" required>
          <option value="">اختر المحافظة</option>
          <option>القاهرة</option>
          <option>الجيزة</option>
          <option>الإسكندرية</option>
          <option>الدقهلية</option>
          <option>الشرقية</option>
          <option>القليوبية</option>
          <option>الغربية</option>
          <option>المنوفية</option>
          <option>كفر الشيخ</option>
          <option>البحيرة</option>
          <option>دمياط</option>
          <option>بورسعيد</option>
          <option>السويس</option>
          <option>الإسماعيلية</option>
          <option>الفيوم</option>
          <option>بني سويف</option>
          <option>المنيا</option>
          <option>أسيوط</option>
          <option>سوهاج</option>
          <option>قنا</option>
          <option>الأقصر</option>
          <option>أسوان</option>
          <option>الوادي الجديد</option>
          <option>مطروح</option>
          <option>شمال سيناء</option>
          <option>جنوب سيناء</option>
          <option>البحر الأحمر</option>
        </select>
      </div>

      <div class="form-group">
        <label>كود الدعوة <span style="color:var(--text-muted);">(اختياري)</span></label>
        <input type="text" name="referralCode" id="regReferral" placeholder="كود الدعوة">
      </div>

      <button type="submit" class="btn btn-primary" id="registerBtn">
        <i class="fas fa-user-plus"></i> إنشاء الحساب
      </button>
    </form>

    <div class="auth-footer">
      لديك حساب بالفعل؟ <a href="/login">تسجيل الدخول</a>
    </div>
  </div>
</section>

<script>
function updateGrades() {
  var stage = document.getElementById('regStage').value;
  var gradeSelect = document.getElementById('regGrade');
  gradeSelect.innerHTML = '<option value="">اختر الصف</option>';
  if (stage === 'إعدادية') {
    gradeSelect.innerHTML += '<option value="الأول الإعدادي">الأول الإعدادي</option>';
    gradeSelect.innerHTML += '<option value="الثاني الإعدادي">الثاني الإعدادي</option>';
    gradeSelect.innerHTML += '<option value="الثالث الإعدادي">الثالث الإعدادي</option>';
  } else if (stage === 'ثانوية') {
    gradeSelect.innerHTML += '<option value="الأول الثانوي">الأول الثانوي</option>';
    gradeSelect.innerHTML += '<option value="الثاني الثانوي">الثاني الثانوي</option>';
    gradeSelect.innerHTML += '<option value="الثالث الثانوي">الثالث الثانوي</option>';
  }
}

function handleRegister(e) {
  e.preventDefault();
  var btn = document.getElementById('registerBtn');
  btn.disabled = true;
  btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> جاري إنشاء الحساب...';

  var name = document.getElementById('regName').value;
  var email = document.getElementById('regEmail').value;
  var phone = document.getElementById('regPhone').value;
  var parentPhone = document.getElementById('regParentPhone').value;
  var password = document.getElementById('regPassword').value;
  var grade = document.getElementById('regGrade').value;
  var stage = document.getElementById('regStage').value;
  var governorate = document.getElementById('regGovernorate').value;
  var referralCode = document.getElementById('regReferral').value;

  if (!name || !email || !parentPhone || !password || !grade || !stage || !governorate) {
    alert('يرجى ملء جميع الحقول المطلوبة');
    btn.disabled = false;
    btn.innerHTML = '<i class="fas fa-user-plus"></i> إنشاء الحساب';
    return false;
  }

  try {
    if (typeof firebase === 'undefined' || !firebase.auth) {
      // Fallback: submit form directly without Firebase Auth
      document.getElementById('registerForm').submit();
      return false;
    }
    firebase.auth().createUserWithEmailAndPassword(email, password)
      .then(function(cred) {
        return cred.user.getIdToken();
      })
      .then(function(token) {
        return fetch('/api/auth/firebase-register', {
          method: 'POST',
          headers: {'Content-Type': 'application/json'},
          body: JSON.stringify({
            idToken: token, name: name, email: email,
            phone: phone, parentPhone: parentPhone,
            grade: grade, stage: stage,
            governorate: governorate, referralCode: referralCode
          })
        });
      })
      .then(function(r) { return r.json(); })
      .then(function(d) {
        if (d.success) {
          window._regEmail = email;
          document.getElementById('registerForm').innerHTML =
            '<div style="padding:30px 10px;text-align:center;">' +
            '<i class="fas fa-envelope-open-text" style="font-size:54px;color:var(--accent);display:block;margin-bottom:14px;"></i>' +
            '<h2 style="font-family:\'Aref Ruqaa\',serif;font-size:22px;font-weight:700;margin-bottom:8px;">تأكيد البريد الإلكتروني</h2>' +
            '<p style="font-size:14px;color:var(--text-light);margin-bottom:18px;">أرسلنا كود تأكيد مكوّن من 6 أرقام إلى بريدك الإلكتروني. أدخله لتفعيل حسابك.</p>' +
            '<div class="form-group"><input type="text" id="verifyCodeInput" placeholder="الكود المكوّن من 6 أرقام" dir="ltr" style="text-align:center;letter-spacing:6px;font-size:20px;"></div>' +
            '<div id="verifyError" style="display:none;background:rgba(220,38,38,0.1);border:1px solid rgba(220,38,38,0.2);border-radius:8px;padding:10px;margin-bottom:12px;font-size:13px;color:var(--danger);"></div>' +
            '<button class="btn btn-primary" id="verifyBtn" onclick="verifyAccount()" style="width:100%;"><i class="fas fa-check"></i> تأكيد الحساب</button>' +
            '<button class="btn" onclick="resendCode()" style="width:100%;margin-top:10px;background:none;border:1px solid var(--border);color:var(--text-muted);">إعادة إرسال الكود</button>' +
            '</div>';
          if (!d.emailSent) {
            var v = document.getElementById('verifyError');
            v.style.display = 'block'; v.style.color = '#a16207';
            v.style.background = 'rgba(234,179,8,0.1)'; v.style.borderColor = 'rgba(234,179,8,0.3)';
            v.textContent = 'تعذر الإرسال التلقائي للإيميل، أعد المحاولة أو تواصل مع الدعم.';
          }
        }
        else { alert(d.error || 'فشل إنشاء الحساب'); location.reload(); }
      })
      .catch(function(err) {
        console.error(err);
        alert(window.getFirebaseErrorMessage(err));
        btn.disabled = false;
        btn.innerHTML = '<i class="fas fa-user-plus"></i> إنشاء الحساب';
      });
  } catch (err) {
    alert('حدث خطأ في الاتصال. تأكد من اتصالك بالإنترنت.');
    btn.disabled = false;
    btn.innerHTML = '<i class="fas fa-user-plus"></i> إنشاء الحساب';
  }
  return false;
}

function verifyAccount() {
  var code = document.getElementById('verifyCodeInput').value.trim();
  var err = document.getElementById('verifyError');
  var btn = document.getElementById('verifyBtn');
  err.style.display = 'none';
  if (!code) { err.style.display = 'block'; err.textContent = 'يرجى إدخال الكود'; return; }
  btn.disabled = true; btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> جاري التأكيد...';
  fetch('/api/auth/verify-email', {
    method: 'POST', headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email: window._regEmail, code: code })
  }).then(function(r) { return r.json(); }).then(function(d) {
    if (d.success) { window.location.href = '/student'; }
    else { err.style.display = 'block'; err.textContent = d.error || 'فشل التأكيد'; btn.disabled = false; btn.innerHTML = '<i class="fas fa-check"></i> تأكيد الحساب'; }
  }).catch(function() {
    err.style.display = 'block'; err.textContent = 'فشل الاتصال بالخادم'; btn.disabled = false; btn.innerHTML = '<i class="fas fa-check"></i> تأكيد الحساب';
  });
}

function resendCode() {
  var err = document.getElementById('verifyError');
  if (!window._regEmail) return;
  fetch('/api/auth/send-verify-code', {
    method: 'POST', headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email: window._regEmail })
  }).then(function(r) { return r.json(); }).then(function(d) {
    if (d.success) {
      err.style.display = 'block'; err.style.color = '#15803d'; err.style.background = 'rgba(34,197,94,0.1)'; err.style.borderColor = 'rgba(34,197,94,0.3)';
      err.textContent = 'تم إرسال كود جديد إلى بريدك الإلكتروني.';
    } else { err.style.display = 'block'; err.style.color = ''; err.style.background = ''; err.style.borderColor = ''; err.textContent = d.error || 'فشل إعادة الإرسال'; }
  }).catch(function() {
    err.style.display = 'block'; err.textContent = 'فشل الاتصال بالخادم';
  });
}
</script>
<%- include('../partials/footer') %>
