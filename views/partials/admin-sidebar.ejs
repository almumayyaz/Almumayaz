  <aside class="sidebar collapsed" id="sidebar">
    <div class="sidebar-header">
      <a href="/admin" class="sidebar-logo">المُميز</a>
    </div>
    <div class="sidebar-user">
      <div class="sidebar-avatar"><%= user ? user.name.charAt(0) : 'م' %></div>
      <div class="sidebar-user-info">
        <h4><%= user ? user.name : 'محمد عفيفي' %></h4>
        <span>المدير</span>
      </div>
    </div>
    <nav class="sidebar-nav">
      <a href="/admin" class="<%= currentPath === '/admin' ? 'active' : '' %>">
        <i class="fas fa-tachometer-alt"></i> <span>لوحة التحكم</span>
      </a>
      <a href="/admin/analytics" class="<%= currentPath === '/admin/analytics' ? 'active' : '' %>">
        <i class="fas fa-chart-bar"></i> <span>تحليلات</span>
      </a>
      <a href="/admin/student-progress" class="<%= currentPath === '/admin/student-progress' ? 'active' : '' %>">
        <i class="fas fa-user-graduate"></i> <span>تقدم الطلاب</span>
      </a>
      <a href="/admin/students" class="<%= currentPath === '/admin/students' ? 'active' : '' %>">
        <i class="fas fa-users"></i> <span>الطلاب</span>
      </a>
      <a href="/admin/courses" class="<%= currentPath === '/admin/courses' ? 'active' : '' %>">
        <i class="fas fa-book"></i> <span>المحاضرات</span>
      </a>
      <a href="/admin/notes" class="<%= currentPath === '/admin/notes' ? 'active' : '' %>">
        <i class="fas fa-sticky-note"></i> <span>المذكرات</span>
      </a>
      <a href="/admin/sub-requests" class="<%= currentPath === '/admin/sub-requests' ? 'active' : '' %>">
        <i class="fas fa-file-invoice"></i> <span>طلبات الاشتراك</span>
      </a>
      <a href="/admin/charge-codes" class="<%= currentPath === '/admin/charge-codes' ? 'active' : '' %>">
        <i class="fas fa-barcode"></i> <span>أكواد الشحن</span>
      </a>
      <a href="/admin/subscriptions" class="<%= currentPath === '/admin/subscriptions' ? 'active' : '' %>">
        <i class="fas fa-credit-card"></i> <span>الاشتراكات</span>
      </a>
      <a href="/admin/payments" class="<%= currentPath === '/admin/payments' ? 'active' : '' %>">
        <i class="fas fa-money-bill"></i> <span>المدفوعات</span>
      </a>
      <a href="/admin/announcements" class="<%= currentPath === '/admin/announcements' ? 'active' : '' %>">
        <i class="fas fa-bullhorn"></i> الإعلانات
      </a>
      <a href="/admin/puter-ai" class="<%= currentPath === '/admin/puter-ai' ? 'active' : '' %>">
        <i class="fas fa-robot"></i> مساعد AI
      </a>
      <a href="/admin/quotes" class="<%= currentPath === '/admin/quotes' ? 'active' : '' %>">
        <i class="fas fa-quote-right"></i> <span>جمل تحفيزية</span>
      </a>
      <a href="/admin/send-notification" class="<%= currentPath === '/admin/send-notification' ? 'active' : '' %>">
        <i class="fas fa-bell"></i> <span>إرسال إشعار</span>
      </a>
      <a href="/admin/notifications" class="<%= currentPath === '/admin/notifications' ? 'active' : '' %>">
        <i class="fas fa-inbox"></i> <span>مركز الإشعارات</span>
        <% if (typeof unreadCount !== 'undefined' && unreadCount > 0) { %><span style="margin-right:auto;min-width:18px;height:18px;padding:0 5px;border-radius:9px;background:var(--danger);color:#fff;font-size:10px;font-weight:700;display:inline-flex;align-items:center;justify-content:center;"><%= unreadCount > 99 ? '99+' : unreadCount %></span><% } %>
      </a>
      <a href="/admin/reviews" class="<%= currentPath === '/admin/reviews' ? 'active' : '' %>">
        <i class="fas fa-star"></i> <span>المراجعات</span>
      </a>
      <a href="/admin/live-sessions" class="<%= currentPath === '/admin/live-sessions' ? 'active' : '' %>">
        <i class="fas fa-video"></i> <span>الحصص المباشرة</span>
      </a>
      <a href="/admin/settings" class="<%= currentPath === '/admin/settings' ? 'active' : '' %>">
        <i class="fas fa-cog"></i> <span>الإعدادات</span>
      </a>
      <a href="/admin/chat" class="<%= currentPath.startsWith('/admin/chat') ? 'active' : '' %>">
        <i class="fas fa-comments"></i> <span>تواصل مع الطلاب</span>
      </a>
      <a href="/admin/dev" class="<%= currentPath === '/admin/dev' ? 'active' : '' %>">
        <i class="fas fa-terminal"></i> <span>لوحة المطور</span>
      </a>
      <div class="sidebar-divider"></div>
      <a href="/">
        <i class="fas fa-globe"></i> <span>الموقع العام</span>
      </a>
      <a href="/logout" class="btn-logout">
        <i class="fas fa-sign-out-alt"></i> <span>تسجيل الخروج</span>
      </a>
    </nav>
  </aside>
  <div class="sidebar-overlay" id="sidebarOverlay"></div>
  <style>
    .sidebar-nav {
      display: flex; flex-direction: column; gap: 2px;
      padding: 8px 12px; flex: 1; overflow-y: auto;
    }
    .sidebar-nav a {
      display: flex; align-items: center; gap: 10px;
      padding: 10px 14px; font-size: 14px; font-weight: 500;
      color: var(--text); text-decoration: none;
      border-radius: var(--radius); transition: all 0.2s;
    }
    .sidebar-nav a i { width: 18px; text-align: center; font-size: 15px; color: var(--text-muted); transition: color 0.2s; }
    .sidebar-nav a:hover { background: var(--accent-glow); }
    [data-theme="light"] .sidebar-nav a:hover { color: #000; }
    [data-theme="dark"] .sidebar-nav a:hover { color: var(--accent); }
    .sidebar-nav a.active { background: var(--accent-glow); font-weight: 700; }
    [data-theme="dark"] .sidebar-nav a.active { color: var(--accent); }
    [data-theme="light"] .sidebar-nav a.active { color: #000; }
    .sidebar-nav a.active i { color: var(--accent); }
    .sidebar-nav a.btn-logout { color: #ef4444 !important; }
    .sidebar-nav a.btn-logout i { color: #ef4444 !important; }
    .sidebar-divider { height: 1px; background: var(--glass-border); margin: 6px 0; }
  </style>
  <script>
    (function() {
      var sidebar = document.getElementById('sidebar');
      var overlay = document.getElementById('sidebarOverlay');

      var isMobile = function() { return window.innerWidth <= 768; };

      function openSidebar() {
        sidebar.classList.add('open');
        if (overlay) overlay.classList.add('show');
        document.body.style.overflow = 'hidden';
      }
      function closeSidebar() {
        sidebar.classList.remove('open');
        if (overlay) overlay.classList.remove('show');
        document.body.style.overflow = '';
      }
      function toggleSidebar() {
        if (sidebar.classList.contains('open')) { closeSidebar(); return; }
        if (isMobile()) { openSidebar(); }
        else {
          sidebar.classList.toggle('collapsed');
          localStorage.setItem('admin-sidebar-toggle', sidebar.classList.contains('collapsed') ? 'collapsed' : '');
        }
      }

      if (!isMobile()) {
        var toggleState = localStorage.getItem('admin-sidebar-toggle');
        if (toggleState === '') { sidebar.classList.remove('collapsed'); }
        else if (toggleState === 'collapsed') { sidebar.classList.add('collapsed'); }
      }

      if (overlay) overlay.addEventListener('click', closeSidebar);

      sidebar.querySelectorAll('.sidebar-nav a').forEach(function(link) {
        link.addEventListener('click', function() {
          if (isMobile()) closeSidebar();
          else {
            sidebar.classList.add('collapsed');
            localStorage.setItem('admin-sidebar-toggle', 'collapsed');
          }
        });
      });
    })();
  </script>
