const { StorageService, generateObjectKey, validateMimeByBytes, generateChecksum, ALLOWED_MIMES, ALLOWED_EXTS } = require('../../infrastructure/storage/StorageService');
module.exports = { StorageService, generateObjectKey, validateMimeByBytes, generateChecksum, ALLOWED_MIMES, ALLOWED_EXTS };
